name: CI — Broken Build (HAL9000-OPS Test Target)

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main"]

# ─────────────────────────────────────────────────────────────────────────────
# THIS WORKFLOW IS INTENTIONALLY BROKEN to trigger HAL9000-OPS investigation.
#
# Bugs planted:
#   1. Wrong Python version string "3.13.99" — does not exist, setup will fail
#   2. requirements file path typo: "app/requirementz.txt" — file not found
#   3. Test command references wrong directory: "testss/" — no such folder
#
# Point your HAL9000-OPS agent at this repo and watch it investigate.
# To fix: see the comments marked [FIX] on each broken line.
# ─────────────────────────────────────────────────────────────────────────────

jobs:
  test:
    name: Run Tests (broken)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.99"   # ❌ BUG 1: version does not exist
                                       # [FIX] change to "3.12"

      - name: Install dependencies
        run: |
          pip install -r app/requirementz.txt   # ❌ BUG 2: typo in filename
                                                 # [FIX] change to app/requirements.txt

      - name: Run tests
        run: |
          pytest testss/ -v   # ❌ BUG 3: wrong directory name
                               # [FIX] change to tests/

  docker-build:
    name: Build Docker Image (broken)
    runs-on: ubuntu-latest
    needs: [test]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: hal9000-ops-target:${{ github.sha }}
